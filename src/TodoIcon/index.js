import React from "react";

function TodoIcon() {
    return (
        <span
            className={`Icon-container Icon-container--${type}`}
            onClick={onClick}>

        </span>
    )

    
}

export {TodoIcon}